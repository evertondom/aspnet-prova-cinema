@model Cinema.Models.Sessao

@{
    ViewData["Title"] = "Detalhes";
}

<h1>Detalhando</h1>

<div>
    <h4>Sessao</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NumSala)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NumSala)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.SessaoId" class="btn btn-primary">Editar</a> |
    <a asp-action="Index" class="btn btn-info">Voltar</a>
</div>
