@model IEnumerable<Cinema.Models.Sessao>

@{
    ViewData["Title"] = "Sessao";
}

<h1>Lista de Salas</h1>

<p>
    <a asp-action="Create" class="btn btn-light">Adicionar</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumSala)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumSala)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SessaoId" class="btn btn-primary">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.SessaoId" class="btn btn-warning">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.SessaoId" class="btn btn-danger">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
