@model Cinema.Models.Cine

@{
    ViewData["Title"] = "Detalhes";
}

<h1>Detalhando</h1>

<div>
    <h4>Cinema</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.LocalCinema)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.LocalCinema)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.CinemaId" class="btn btn-primary">Editar</a> |
    <a asp-action="Index" class="btn btn-info">Voltar</a>
</div>
